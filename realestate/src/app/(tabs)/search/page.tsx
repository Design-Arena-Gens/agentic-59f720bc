import { properties } from "@/data/properties";
import { SearchExperience } from "@/components/search/search-experience";

export default function SearchPage() {
  return <SearchExperience properties={properties} />;
}
